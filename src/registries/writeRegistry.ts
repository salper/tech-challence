import { Booking } from "../types/Booking.js";
import { Room } from "../types/Room.js";

// Expose these storages through appropriate interfaces

const bookings: Booking[] = [];

const rooms: Room[] = []; // Feed with provided data
